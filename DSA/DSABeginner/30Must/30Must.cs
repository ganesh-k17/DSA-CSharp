using System;
using System.Collections.Generic;
using System.Linq;
using System.Security;
using System.Text;
using System.Threading.Tasks;

namespace DSABeginner._30Must
{
    internal class _30Must
    {
        public static string AdditionOfBinaryString(string a, string b)
        {
            string result = string.Empty;
            int aLen = a.Length;
            int bLen = b.Length;
            int i = aLen > bLen ? aLen-1 : bLen-1;
            while (i < 0)
            {

            }
            return result;
        }
    }
}
